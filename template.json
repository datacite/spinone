{
  "builders": [{
    "type": "amazon-ebs",
    "access_key": "{{user `access_key`}}",
    "secret_key": "{{user `secret_key`}}",
    "region": "{{user `region`}}",
    "source_ami": "{{user `source_ami`}}",
    "instance_type": "{{user `instance_type`}}",
    "ssh_username": "ubuntu",
    "ami_name": "spinone {{timestamp}}"
  }],
  "provisioners": [{
    "type": "shell",
    "only": ["virtualbox-iso"],
    "execute_command": "echo 'vagrant'|sudo -S sh '{{.Path}}'",
    "scripts": [
      "scripts/base.sh",
      "scripts/vagrant.sh",
      "scripts/virtualbox.sh",
      "scripts/cleanup.sh",
      "scripts/zerodisk.sh"
    ]
  },
  {
    "type": "chef-solo",
    "cookbook_paths": ["vendor/cookbooks"],
    "run_list": ["recipe[passenger_nginx]"],
    "override": {
      "amazon-ebs": {
        "json": {
          "ruby": {
            "deploy_user": "ubuntu",
            "deploy_group": "ubuntu",
            "rails_env": "production"
          },
          "application": "spinone",
          "phantomjs": {
            "version": "1.9.7",
            "base_url": "https://bitbucket.org/ariya/phantomjs/downloads"
          }
        }
      }
    }
  }],
  "post-processors": [{
    "type": "atlas",
    "artifact": "datacite/spinone",
    "artifact_type": "amazon.ami",
    "token": "{{user `atlas_token`}}",
    "metadata": {
      "created_at": "{{timestamp}}"
    }
  },
  {
    "type": "vagrant",
    "compression_level": "{{user `compression_level`}}",
    "only": ["virtualbox-iso"],
    "output": "spinone-virtualbox.box"
  }],
  "variables": {
    "compression_level": "6",
    "cpus": "2",
    "disk_size": "40000",
    "headless": "false",
    "memory": "2048",
    "mirror": "http://releases.ubuntu.com",
    "version": "0.1",
    "access_key": "{{env `AWS_ACCESS_KEY_ID`}}",
    "secret_key": "{{env `AWS_SECRET_ACCESS_KEY`}}",
    "region": "{{env `AWS_REGION`}}",
    "source_ami": "{{env `AWS_SOURCE_AMI`}}",
    "instance_type": "{{env `AWS_INSTANCE_TYPE`}}",
    "atlas_token": "{{env `ATLAS_TOKEN`}}"
  },
  "push": {
    "name": "datacite/spinone",
    "vcs": true
  }
}
